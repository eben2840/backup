 <div class="row">
{% for item in items %}

<div class="col-6 col-sm-4 col-md-4">
  <div class="card">
      <img src="../static/items/{{item.image}}" style="object-fit: cover;" alt="">
      <h4>{{item.name}}</h4>
      <h6>{{item.price}}</h6>
  </div>
</div>

{% endfor %}

  </div>